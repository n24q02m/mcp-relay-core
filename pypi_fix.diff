<<<<<<< SEARCH
      - name: Build and publish
        run: |
          cd packages/core-py
          uv build
          uv publish
=======
      - name: Build and publish
        env:
          UV_PUBLISH_TOKEN: ${{ secrets.PYPI_TOKEN }}
        run: |
          cd packages/core-py
          VERSION=$(grep '^version = ' pyproject.toml | cut -d '\"' -f 2)
          if curl -s -f \"https://pypi.org/pypi/mcp-relay-core/$VERSION/json\" > /dev/null 2>&1; then
            echo \"mcp-relay-core@$VERSION already published, skipping\"
          else
            uv build
            uv publish
          fi
>>>>>>> REPLACE
